import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    zIndex: 1000,
    elevation: 10,
    justifyContent: 'space-between',
    alignItems: 'center',
  },
})
