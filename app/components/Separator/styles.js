import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  SeparatorStyle: {
    borderStyle: 'solid',
    width: 0.5,
    alignSelf: 'center',
    justifyContent: 'center',
  },
});
